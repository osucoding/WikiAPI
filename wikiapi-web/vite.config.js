import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    strictPort: true,
    // TODO: Add proxy settings later: https://vitejs.dev/config/server-options#server-proxy
  }
})
